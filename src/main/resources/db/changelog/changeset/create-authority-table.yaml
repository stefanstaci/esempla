databaseChangeLog:
  - changeSet:
      id: create-authority
      author: Stefan
      changes:
        - createTable:
            tableName: authority
            columns:
              - column:
                  name: name
                  type: varchar(50)
                  defaultValue: User
                  constraints:
                    primaryKey: true
                    nullable: false